import React from 'react';
import ReactDOM from 'react-dom';
import SuccessPage from './SuccessPage';

document.addEventListener('DOMContentLoaded', () => {
  ReactDOM.render(<SuccessPage />, document.getElementById('success-page-root'));
});
